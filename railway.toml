[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @planfortwo/api build"

[deploy]
startCommand = "cd apps/api && npx tsx dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
